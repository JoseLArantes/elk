input {
  beats {
    port => 5044
  }
  
  # Optional: Listen for syslog
  # tcp {
  #   port => 5000
  #   type => syslog
  # }
  # udp {
  #   port => 5000
  #   type => syslog
  # }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  
  # Optional: stdout for debugging
  # stdout { codec => rubydebug }
}
